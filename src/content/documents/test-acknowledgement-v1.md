---
title: "[E2E TEST] Sample Acknowledgement — season 2025"
document: test-acknowledgement
version: 1
kind: acknowledgement
audience: all-members
season: 2025
status: published
---

# E2E test fixture — sample acknowledgement (season 2025)

This document exists only for `e2e/waivers-signing.spec.ts` and
`e2e/waivers-visual.spec.ts`. It is not a real Alaska Sailing Club rule set, and
it never reaches a real member: `season: 2025` is permanently in the past, so no
production `current_season` ever resolves it.

This is the suite's second personal document, alongside `test-release`, so a
signing moment with more than one entry has real content to render — the "one
signed receipt, one expanded document" mid-flow state.
